# 团建活动网页项目规范

## 项目目标

制作并发布 2026-09-01 利雅得团建活动攻略网页，优先保证手机端可读性、导航便利性和预算信息准确性。

## 目录与文件

- `index.html`：页面内容和语义结构。
- `styles.css`：视觉样式、响应式布局和动效。
- `app.js`：语言切换、预算切换、复制信息和本地清单状态。
- `README.md`：项目说明、预览和发布方式。
- 不引入构建工具；页面必须能直接通过 GitHub Pages 静态托管。

## 沙特落地指南扩展

- `saudi-guide/`：独立的外派办事指南，原团建页面保留原入口。
- `saudi-guide/index.html`、`styles.css`、`app.js`：指南结构、手机优先样式和交互。
- `saudi-guide/places.json`：经过来源核实的地点坐标与道路路线参考数据；注明来源和日期，不把直线距离当成导航距离。
- `saudi-guide/vendor/`：仅保存交互地图所需的固定版本 Leaflet 及其许可；不引入构建工具。
- `saudi-guide/README.md`：指南维护方式、路线口径和资料来源。
- 个人经验、估计费用和未证实的流程须就地标注；不记录个人证件号、手机号或保险表。
- 新指南按个人办事口径呈现，不适用团建的 10 人预算和 15 分钟时间粒度。
- 发布继续使用现有 GitHub Pages Actions；临时研究与下载文件只放系统临时目录，不提交。

## 内容规则

- 行程时间使用 UTC+3，最细粒度为 15 分钟。
- 所有地址、预订入口使用可点击链接。
- 费用默认按 10 人计算，活动总预算不超过 SAR 200/人。
- 中英文内容含义保持一致；价格变化或未完全确认的信息必须明确提示。

## 开发与验证

- 使用原生 HTML、CSS、JavaScript，不依赖第三方运行时。
- 修改后检查桌面端和移动端布局、外链、一键复制、语言切换及预算计算。
- 不主动创建 Git commit；仅在用户明确要求发布时，为发布所需提交并推送。
